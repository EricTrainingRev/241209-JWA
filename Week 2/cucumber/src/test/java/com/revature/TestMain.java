package com.revature;

import io.cucumber.core.cli.Main;

public class TestMain {
    public static void main(String[] args) {
        /*
            You can always call the Cucumber cli Main method directly
            if you are having issues using a plugin to execute your tests,
            or if you want to control test execution via a wrapper class
         */
        Main.main(args);
    }
}
